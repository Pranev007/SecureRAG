#!/bin/sh
# Refuse to commit a credential. Enable with:
#     git config core.hooksPath .githooks
#
# Bypassable with `git commit --no-verify`, like every hook -- it is a seatbelt
# against the accident, not a control against a determined author.
python scripts/scan_secrets.py || exit 1
