# Optional, heavier capabilities. NOT required to run SecureRAG.
# Each has a deterministic built-in fallback; see docs/security.md.

# Higher-recall PII detection (PII_ENGINE=presidio).
# Also requires: python -m spacy download en_core_web_lg
presidio-analyzer==2.2.357
presidio-anonymizer==2.2.357

# Cross-encoder reranking (RERANKER=cross_encoder) *and* NLI grounding
# verification (GROUNDING_METHOD=nli|hybrid). One dependency, two features.
# Pulls in torch (~2 GB). The heuristic reranker and the lexical grounding
# verifier are the defaults, and both features degrade to them cleanly:
# the report's `method` field records which one actually ran.
#
# The NLI checkpoint (NLI_MODEL, ~750 MB) downloads on first use. Set HF_HOME
# to move the cache off the system drive.
#   sentencepiece is needed for the DeBERTa-v3 tokenizer.
sentence-transformers>=3.3,<6
sentencepiece>=0.2
